package com.example.springsocial.util;

/**
 * @author Yazeed Hammad - yazeed.hammad@erabia.com
 *
 *         Jan 8, 2019
 */

public class DocxUtil {

	/*
	 * XWPFDocument doc = new XWPFDocument( OPCPackage.open(
	 * "/home/yhammad/Downloads/Two-page-bordered-cv-template-alternative.docx"));
	 * for (XWPFParagraph p : doc.getParagraphs()) { List<XWPFRun> runs =
	 * p.getRuns(); if (runs != null) { for (XWPFRun r : runs) { String text =
	 * r.getText(0); if (text != null && text.contains("James Smith")) { text =
	 * text.replace("James Smith", "Yazeed"); r.setText(text, 0); } } } } for
	 * (XWPFTable tbl : doc.getTables()) { for (XWPFTableRow row : tbl.getRows()) {
	 * for (XWPFTableCell cell : row.getTableCells()) { for (XWPFParagraph p :
	 * cell.getParagraphs()) { for (XWPFRun r : p.getRuns()) { String text =
	 * r.getText(0); if (text != null && text.contains("OVERVIEW")) { text =
	 * text.replace("OVERVIEW", "يزيد\n\n\n\nYYYY"); r.setText(text, 0); } } } } } }
	 * doc.write(new FileOutputStream("/home/yhammad/Downloads/cv.docx"));
	 * 
	 * <dependency> <groupId>org.apache.poi</groupId> <artifactId>poi</artifactId>
	 * <version>3.17</version> </dependency>
	 * 
	 * <!-- https://mvnrepository.com/artifact/org.apache.poi/poi-ooxml -->
	 * <dependency> <groupId>org.apache.poi</groupId>
	 * <artifactId>poi-ooxml</artifactId> <version>3.17</version> </dependency>
	 *
	 */
}
